const CountryInformation = (props) =>{
    return (    
        <div>
        <p>capital {props.capital}</p>
        <p>area {props.area}</p>
        </div>
    
)
}

export default CountryInformation